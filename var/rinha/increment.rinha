let generateIncrement = fn(increment) => {
    fn(x) => {
        x + increment
    }
};

let incrementBy5 = generateIncrement(5);
let result = incrementBy5(10);
print(result)  // The result will be 15
